# Generate blueprint.json for a skill

## Objective

Inspect the target skill directory, understand the real tool contract and environment, and generate a valid planner blueprint JSON file.

## Directory placeholders

- Target skill directory: `{SKILL_DIR}`
- Output blueprint path: `{OUTPUT_PATH}`

Output blueprint path: `{OUTPUT_PATH}`

## Persona JSON

{PERSONA_CONTENT}

## Available capabilities

{AVAILABLE_CAPABILITIES}

## Files to inspect first

{REQUIRED_FILES_GUIDE}

## Required workflow

{PLANNING_WORKFLOW}

## Blueprint rules

{BLUEPRINT_RULES}

## Scenario alignment rules

{SCENARIO_ALIGNMENT_RULES}

## Fallback policy

{FALLBACK_POLICY}

## Output schema

The JSON you write must follow this schema:

{OUTPUT_SCHEMA}

## Execution requirements

1. Inspect the real skill files under `{SKILL_DIR}` before writing anything.
2. Use the final repository state as ground truth, even if some files were generated by different tools.
3. Write the final blueprint JSON to `{OUTPUT_PATH}`.
4. The output file must contain only one JSON object, with no markdown fences or commentary.
5. Do not modify any files other than `{OUTPUT_PATH}`.
6. Before finishing, re-open the written file and ensure the JSON is valid and tool names match `tools.jsonl`.
7. This task is not complete until `{OUTPUT_PATH}` exists on disk and contains valid JSON.
8. Do not stop after analysis; you must actually create or overwrite `{OUTPUT_PATH}`.
