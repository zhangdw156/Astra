# Astra — Main configuration entry
# All components are configurable via this hierarchy

defaults:
  - _self_
  - skill: default
  - agent: default
  - env: default

# Global settings
project:
  name: astra
  version: "0.1.0"
