{
  "defaults": {
    "show_summary": true,
    "auto_detect_urls": true,
    "default_mode": "single"
  },
  "notes": {
    "show_summary": "Auto-show summary after fetching (true/false)",
    "auto_detect_urls": "Auto-detect tweet URLs in messages (true/false)",
    "default_mode": "Fetch mode: 'single' only in lite version"
  }
}
