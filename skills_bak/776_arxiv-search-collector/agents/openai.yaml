interface:
  display_name: "ArXiv Search Collector"
  short_description: "Plan queries and collect deduped arXiv metadata into dirs"
  default_prompt: "Collect arXiv candidates into a base directory from user search parameters."
