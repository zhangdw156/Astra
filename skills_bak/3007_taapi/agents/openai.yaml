interface:
  display_name: "TAAPI.IO Wrapper"
  short_description: "TAAPI.IO market indicators with explicit secret handling"
  default_prompt: "Use $taapi to fetch TAAPI.IO indicators through the local CLI while respecting its TAAPI_SECRET and endpoint safety requirements."
