{
  "name": "cleanapp_ingest_v1",
  "version": "1.0.1",
  "description": "Bulk submit problem signals (bugs/incidents/feedback) to CleanApp via /v1/reports:bulkIngest (quarantine by default).",
  "entrypoint": "run.sh",
  "secrets": [
    "CLEANAPP_API_TOKEN"
  ],
  "notes": {
    "data_handling": "Submits text + optional approximate location. No media by default. Supports dry-run.",
    "network": "HTTPS only (CleanApp ingest endpoint)."
  }
}
