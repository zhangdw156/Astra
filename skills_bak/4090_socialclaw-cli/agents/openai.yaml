interface:
  display_name: "SocialClaw CLI"
  short_description: "Agentic control plane for safe Meta workflows via social CLI."
  default_prompt: "Use socialclaw-cli to plan multi-step Meta operations, map intent to social commands, and gate risky actions with explicit confirmations."
