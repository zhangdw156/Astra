interface:
  display_name: "OpenD Trading"
  short_description: "OpenD trading and market-data workflows"
  default_prompt: "Use $opend to inspect OpenD accounts, snapshots, positions, or simulated orders with secret-ref based credential handling."

policy:
  allow_implicit_invocation: true
