services:
  portfolio-risk-analyzer:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: portfolio-risk-analyzer-mcp
    ports:
      - "8000:8000"
      - "8001:8001"
    environment:
      - MCP_TRANSPORT=http
      - PORTFOLIO_API_BASE=http://localhost:8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

# Usage:
# 1. Build: docker compose -f docker/docker-compose.yaml build
# 2. Start: docker compose -f docker/docker-compose.yaml up -d
# 3. Logs: docker compose -f docker/docker-compose.yaml logs -f
# 4. Stop: docker compose -f docker/docker-compose.yaml down
#
# MCP service: http://localhost:8000