# med-info docs

- `RESOURCES.md`: Catalog of public/open sources that can be integrated into med-info (and what each adds).
- `PROBE_RESULTS.md`: Summary of the latest automated probe run (see artifacts for JSON).
