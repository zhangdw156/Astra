# Publish Safety Notes

- Keep this skill generic: no personal names, secrets, private infrastructure paths, or account IDs.
- Use configurable `--root` and `--out` arguments.
- Keep approval-gate language explicit: no silent deletions.
