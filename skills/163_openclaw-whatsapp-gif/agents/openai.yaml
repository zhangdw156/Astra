interface:
  display_name: "OpenClaw WhatsApp GIF"
  short_description: "Find and send safe WhatsApp reaction GIFs"
  default_prompt: "Use $openclaw-whatsapp-gif to source and send one safe reaction GIF in WhatsApp."
policy:
  allow_implicit_invocation: true
