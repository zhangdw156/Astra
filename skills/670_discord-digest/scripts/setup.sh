#!/bin/bash
# Discord Digest Skill — Setup
# No external dependencies required! Uses only Python stdlib (urllib, json).
echo "✅ Discord Digest Skill — no additional dependencies needed."
echo "   Uses Python 3 stdlib only (urllib.request, json)."
echo ""
echo "Verify Python 3 is available:"
python3 --version 2>/dev/null || echo "❌ Python 3 not found! Install it first."
