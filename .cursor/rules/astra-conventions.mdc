---
description: Astra 项目约定：提交信息、配置、文档与包内引用
alwaysApply: true
---

# Astra 项目约定

## 提交信息 (Commit Message)

- **必须使用英文**，且符合 [Conventional Commits](https://www.conventionalcommits.org/)。
- 格式：`<type>(<scope>): <subject>`，常见 type：`feat`、`fix`、`docs`、`refactor`、`chore`、`test`。
- 示例：`feat(skill): add OmegaConf for repos.yaml`、`fix(sync): use relative paths in .gitmodules`。

## YAML 与配置（必须用 Hydra）

- 所有涉及 **YAML 配置** 的入口必须走 **Hydra 框架**：**使用 `@hydra.main()` 装饰入口函数**，由 Hydra 负责加载配置（config_path / config_name），业务逻辑只接收 `DictConfig`。

## 注释语言

- **所有注释（含 docstring、行内注释）均以中文为准**，便于团队阅读与维护。

## 文档语言

- **除 `.github/` 目录外**，项目内文档（README、docs、各目录下的 README 等）**以中文为主**。
- `.github/` 下 Issue / PR 模板等保持英文。

## src/astra 作为包使用

- **`src/astra` 是供外部调用的 package**，主要在 **`exps/`** 等入口被使用。

## 包内引用 (src/astra)

- **优先使用相对引用**：  
  `src/astra/` 下的模块彼此引用时，尽量使用 `from .foo import Bar`、`from ..agent import PlannerAgent` 这类相对导入，便于在包内部重构/移动模块。

- **对外只暴露一到两级目录，更深层实现放在下划线包中**：  
  推荐模式参考 `astra.agent`、`astra.utils`：
  - 顶层 API：通过 `astra.agent`、`astra.utils` 等一级/二级包对外暴露公共类与函数（例如 `from astra.agent import PlannerAgent`、`from astra.utils import setup_logging`）。
  - 内部实现：放在 `_xxx` 子包或内部模块中（如 `astra.agent._planner_agent`），由顶层 `__init__.py` 统一导出，不建议外部直接依赖 `_xxx` 包路径。

- **配置与公共工具模块集中管理**：  
  - **日志与输出**：统一通过 `astra.utils` 暴露的 API 使用（如 `setup_logging`、`welcome_dashboard`、`print_trace`），内部实现细节放在 `src/astra/utils/` 内部模块中。  
  - **配置**：通过 Hydra 与项目根目录 `configs/` 获取配置，不散落硬编码。
